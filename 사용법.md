# 가정 생활비 가계부 자동화 사용법

이 프로젝트는 [260902_autobudget](개인 지출 가계부) 프로젝트를 그대로 복사해서 시작한
"가정 생활비" 전용 가계부예요. 코드 구조와 사용 방법은 똑같습니다.

## 현재 상태
- **우리은행 / 현대카드 / 네이버페이 / 쿠팡 / 쿠팡이츠 / 구글플레이 / 토스뱅크**: 파서가 이미 있어서 바로 처리됨
- **모임통장 / 모임 체크카드**: 아직 전용 파서가 없음. 파일명에 이 글자가 들어있으면
  프로그램이 알아보긴 하지만 "아직 지원하지 않는 형식입니다"라고 안내만 하고 건너뜀.
  → 실제 파일을 `input` 폴더에 넣고 Claude Code한테 "이 파일도 지원되게 해줘"라고
    요청하면 파일 형식을 보고 만들어줌.

### 토스뱅크 파일 비밀번호 안내
토스뱅크에서 받은 거래내역 엑셀 파일은 보통 비밀번호가 걸려있습니다.
`가계부_업데이트.bat` 실행 중 검은 창에 비밀번호를 물어보면 입력하고 Enter를 눌러주세요
(입력한 글자가 화면에 그대로 보입니다). 비밀번호는 저장하지 않으므로 실행할 때마다
매번 입력해야 합니다.

## 준비 (최초 1회만)
1. 이 컴퓨터에 파이썬(Python)이 설치되어 있어야 합니다.
2. 폴더에서 `Shift + 마우스 우클릭` → "여기에 PowerShell 창 열기" → 아래 입력
   ```
   pip install -r requirements.txt
   ```

## 매달 하는 일
1. 은행 앱/홈페이지에서 이번 달 거래내역 파일(엑셀/PDF)을 받는다.
2. 받은 파일들을 전부 `input` 폴더 안에 복사해 넣는다.
3. `가계부_업데이트.bat` 파일을 더블클릭한다.
4. 검은 창에 처리 결과가 나온다. `output` 폴더의 `yymm00_생활비가계부_claudecli.xlsx`
   (예: 8월 거래면 `260800_생활비가계부_claudecli.xlsx`)를 열어서 확인한다.
   yymm00은 이번에 넣은 파일들 중 가장 최근 거래월 기준으로 자동으로 정해진다.
   지난달 파일은 그대로 남아있고, 이번 달 파일만 새로 만들어지거나 갱신된다.

## 나머지
자세한 규칙(메모 자동 채우기, memo_mapping.csv 사용법, 카테고리 자동 분류 등)은
개인 가계부 프로젝트의 `사용법.md`와 동일하니 참고하세요.
